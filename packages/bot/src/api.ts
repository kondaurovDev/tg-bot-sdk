/**
 * @module api
 *
 * Re-export of `@effect-ak/tg-bot-api` under the `@effect-ak/tg-bot/api`
 * subpath, so that a bot only needs a single package installed.
 */
export * from "@effect-ak/tg-bot-api"
