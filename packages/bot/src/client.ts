/**
 * @module client
 *
 * Re-export of `@effect-ak/tg-bot-client` under the `@effect-ak/tg-bot/client`
 * subpath, so that a bot only needs a single package installed.
 */
export * from "@effect-ak/tg-bot-client"
