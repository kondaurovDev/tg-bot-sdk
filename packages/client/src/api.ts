/**
 * @module api
 *
 * Re-export of `@effect-ak/tg-bot-api` under the
 * `@effect-ak/tg-bot-client/api` subpath, so that a client-only consumer
 * needs a single package installed.
 */
export * from "@effect-ak/tg-bot-api"
